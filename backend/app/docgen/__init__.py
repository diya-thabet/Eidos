"""
Auto-documentation generation engine (Phase 6).

Modules:
    models       - DocSection, DocTemplate, GeneratedDocument
    templates    - Template definitions for each document type
    generator    - Deterministic doc generation from graph + summaries
    renderer     - Markdown rendering with citations and symbol links
    orchestrator - Pipeline: fetch data -> generate -> persist
"""
